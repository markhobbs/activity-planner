/*
describe('Nav Links', () => {
  test('"Home" icon link points to the correct page', () => {});
  test('"Basket" badge link points to the activity planner page', () => {});
});

describe('Footer Links', () => {
  test('"About" link points to the correct page', () => {});
  test('"Store" link points to the correct page', () => {});
  test('"Contact" link points to the correct page', () => {});
});

describe('Activity Card', () => {
  test('"Featured", display featured Icon (Star)', () => {});
  test('"Featured" add border gold to Card', () => {});
  test('"Completed", reduce transparency of Card', () => {});
  test('"Completed" Activity set "Add" button label to "Reapply"', () => {});
  test('BROKEN "Reapply" button sets Card Transparency to 100%', () => {
    //NOT WORKING state change required in items
  });
  test('BROKEN "Reapply" button sets label to Add', () => {
    //NOT WORKING state change required in items
  });
  test('If image show image in Card', () => {});
  test('If Heading show heading text', () => {});
  test('If Summary show summary text', () => {});
  test('"Add" link updates adds 1 to the Activity Basket Icons Total', () => {});
});

describe('Activity Card Required Kit', () => {
  test('If card "KIT" Required, display "Kit" list', () => {});
  test('If card "KIT" Required, display buy "Kit" button', () => {});
  test('Buy "KIT" button points to Store', () => {});
});

describe('Activity Card Rewards', () => {
  test('If Rewards show chips for each Reward', () => {});
  test('If Rewards" show correct color for each chips value', () => {});
});

describe('Planner', () => {
  test('If No activities, show "no records" message', () => {});
  test('"add another activity" button link to activity page', () => {});
  test('if activities in Basket show activities', () => {});
  test('if activities show image', () => {});
  test('if activities show title', () => {});
  test('if activities show Set Configurator', () => {});

  test('Set Configurator "+" link updates rep placeholder', () => {});
  test('Set Configurator "+" link updates Total placeholder', () => {});
  test('Set Configurator "-" link updates rep placeholder', () => {});
  test('Set Configurator "-" link updates Total placeholder', () => {});
  test('Set Configurator "-" link when no items remove activity from list', () => {});

  test('"Complete" button changes label to "Completed"', () => {});
  test('"Complete" button re-calculate Activities "Total"', () => {});
  test('"Reapply" action sets button label from "completed" to "complete"', () => {});
  test('"Delete" button recalculate "Total"', () => {});
  test('"Delete" button activity from list', () => {});
});

describe('Total Widget', () => {
  test('When activity completed add to  "Total Completed"', () => {});
  test('When activity removed delete from "Total Completed"', () => {});
  test('When  "Total Added" and "Total Completed" update "Total Percentage"', () => {});
  test('When  "Total Added" == "Total Completed" change background to gold', () => {});
});

describe('Score Card and History', () => {
  test('On "History" page load List all Awards Badges Chronologically"', () => {});
  test('On "Score" page load list all Awards Badges and scores Chronologically"', () => {});
});

describe('Reset Button', () => {
  test('Returns App State to initState', () => {});
});
*/